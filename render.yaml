services:
  - type: web
    name: portfolio-backend
    env: rust
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DATABASE_URL
        value: "postgres://postgres.epwwoaafrosvqerlkbwp:Alfa1bravo2charlie1!@aws-0-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require"
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
      - key: FRONTEND_URL
        value: https://williamfrank.vercel.app